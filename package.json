{
  "name": "bifrostapi-mcp",
  "version": "0.3.0",
  "description": "MCP server exposing BifrostAPI image generation as a tool. Two transports: stdio (server.js, runs locally, key from env) and remote Streamable HTTP (http-server.js, hosted multi-tenant endpoint, key per-request). Async job + poll so long generations never hit the host's tool-call timeout.",
  "type": "module",
  "bin": {
    "bifrostapi-mcp": "server.js",
    "bifrostapi-mcp-http": "http-server.js"
  },
  "files": [
    "server.js",
    "http-server.js"
  ],
  "scripts": {
    "start:http": "node http-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^5.2.1"
  }
}
